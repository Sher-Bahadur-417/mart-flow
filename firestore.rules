rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Browser clients must not read or write application data.
    // All MartFlow data access goes through Next.js server actions
    // using the Firebase Admin SDK.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
